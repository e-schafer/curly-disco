# curly-disco

Creating a trading bot with Python, Uv, Binance, and NiceGui.

### Log in to the GitHub Container Registry

```shell
export CR_PAT=ghp_...
echo $CR_PAT | docker login ghcr.io -u USERNAME --password-stdin
```
